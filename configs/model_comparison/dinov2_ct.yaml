# =============================================================================
# Model Comparison: DINOv2-CT (patch_size=14, img_size=518, HF transformers)
# =============================================================================
# HuggingFace: Kentucky-Open-Science/Finetuned-DINOv2-Chest-CT
# Architecture: DINOv2 ViT-Large with registers, patch_size=14, img_size=518
# Has 4 register tokens. Uses HuggingFace transformers (not timm).
# 256 is NOT divisible by 14 → resize to 252 (nearest).
# =============================================================================

# --- Model Selection ---
model_key: dinov2_ct

# --- Model Specification (mirrors BACKBONE_SPECS in hf_backbone_loader.py) ---
model:
  repo_id: "Kentucky-Open-Science/Finetuned-DINOv2-Chest-CT"
  backbone_type: "hf"
  patch_size: 14
  native_img_size: 518
  in_chans: 1
  embed_dim: 1024
  num_register_tokens: 4

# --- Dataset ---
dataset:
  clip_min: -997.0
  clip_max: 888.0
  mean_hu: -142.39
  std_hu: 360.97
  num_channels: 1

# --- Data Source ---
validation:
  dataset_format: "npy"
  data_dir: /app/project/ibi-staff/CT-JEPA/Process_CT-RATE/dataset/CT-RATE_valid_hu/
  label_csv: null
  num_workers: 8

# --- Volume List (generated by extract_model_comparison_groundtruth.py) ---
volume_list: /app/project/ibi-staff/CT-JEPA/public/outputs/model_comparison_groundtruth/volume_names.json

# --- Preprocessing Method: cropped_256 ---
# Body-cropped square ROI, resized to 252x252 (patch-aligned for patch_size=14).
# 252 = floor(256 / 14) * 14 = 18 * 14
methods:
  cropped_256:
    enabled: true
    crop: true
    resize_target: 256
    patch_aligned_target: 252
    extract_patches: true
    description: "Body-cropped square ROI, resized to 252x252 (patch-aligned for patch_size=14), CLS + patch tokens"

# --- Output ---
output_folders:
  main_output: /app/project/ibi-staff/CT-JEPA/public/outputs/model_comparison_embeddings/

# --- Required Placeholders ---
distribution:
  type: "ddp"
projector:
  proj_dim: 128
  proj_hidden_dim: 2048
lora_config:
  lora_r: 8
  lora_alpha: 16
  lora_dropout: 0.1
